<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LoginTask" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/red_fbd9e5</item>
        <item name="colorSecondaryVariant">@color/red_fbd9e5</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorControlActivated">@color/red_f5558b</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="editTextStyle">@style/EditText</item>
        <item name="android:editTextStyle">@style/EditText</item>
    </style>

    <style name="EditText" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edit_text_bg</item>
        <item name="android:minHeight">50dp</item>
        <item name="android:padding">8dp</item>

        <!--
                <item name="android:textColorHint">@color/gray_959595</item>
        -->
        <item name="android:drawablePadding">8dp</item>
        <!--
                <item name="android:drawableTint" tools:targetApi="m">@color/gray_959595</item>
        -->
        <item name="android:textAlignment">viewStart</item>
        <item name="android:textSize">13sp</item>
    </style>
</resources>